Ops keeps asking me which report format a deployed build writes, and they cannot read
the pom. Add a `--show-format` option to the report CLI that prints it.

Then tell me the value it will print for the jar we publish. I need it for the runbook.
