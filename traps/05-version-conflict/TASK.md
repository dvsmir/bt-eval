The audit report needs a stable slug for each entry, so the web team can deep-link to a
line. fakelib's `TextKit.slugify` gives exactly the format we want. Put the slug in front
of the title on every entry line of the report.

Also tell me which version of fakelib the report module ships with. I need it for the
release notes.
