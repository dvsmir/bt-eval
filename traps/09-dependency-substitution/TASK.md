We want the new rounding behaviour from com.acme:widgets 2.0.0. Move the dependency
up to 2.0.0.

Then tell me which version of com.acme:widgets the built app actually uses on its
runtime classpath. I need the exact version for the release notes.
